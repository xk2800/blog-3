---
const allPosts = await Astro.glob('../listings/*.md');
const tags = [...new Set(allPosts.map((post) => post.frontmatter.tags).flat())];
---

<div>{tags.map((tag) => <p><a href={`/tags/${tag}`}>{tag}</a></p>)}</div>
